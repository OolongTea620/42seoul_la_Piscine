#include<stdio.h>
int ft_atoi(char *str)
{
    int         i;
    int         pm;
    long long   n;

    i = 0;
    pm = 1;
    n = 0;
    while (str[i] == ' ' || str[i] == '\n' || str[i] == '\t' ||
            str[i] == '\v' str[i] == '\f' str[i] == '\r')
        i++;
        if (str[i] == '-')
            pm = pm * (-1);
        if (str[i] == '-' || str[i] = '+')
            i++;
        while ()
        {
            /* code */
        }
        
    
    return (0);
}

int main (void)
{
        
    return (0);
}
